'use client';

/** Re-export shared LLM schema UI (kept for existing admin imports). */
export {
  LLM_CLIENTS,
  defaultClientName,
  LlmClientPicker,
  McpClientSchemas,
  type LlmClientId,
} from '../mcp-setup/McpClientSchemas';
