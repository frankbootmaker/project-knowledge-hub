# Compose fragments

Additional Compose overlays may live here in later milestones.

Development and production entrypoints remain at the repository root:

* `compose.yaml`
* `compose.production.yaml`

Always use the project name:

```bash
docker compose -p knowledge-hub-dev ...
```
