# @project-knowledge-hub/embeddings

Optional embedding providers, Markdown chunking, and knowledge-record reindex helpers (Milestone 10).

Default provider is **`disabled`** — full-text search continues to work with no external calls. Enable `ollama` or `openai_compatible` via env to index chunks and allow hybrid search.
